name: umvvm

description: UMvvm base classes

version: 4.0.0

publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  dio: ^5.4.0
  async: ^2.11.0
  source_gen: ^1.4.0
  visibility_detector: ^0.4.0+2

dev_dependencies:
  build_runner: ^2.4.7
  build_config: ^1.1.1
  flutter_lints: ^3.0.1
  http_mock_adapter: ^0.6.1

  test: any
  flutter_test: 
    sdk: flutter
  integration_test: 
    sdk: flutter

flutter:

