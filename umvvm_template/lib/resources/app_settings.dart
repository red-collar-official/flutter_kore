class AppSettings {
  static const defaultRequestTimeoutInSeconds = 60;
  static const toastDuration = Duration(milliseconds: 2700);
  static const requestRetryCount = 1;

  // TODO: place app settings here
}
